{
  "name": "poc-vue-authenticate",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "lint": "eslint . --format unix",
    "api-tests": "docker-compose run --rm api-tests"
  },
  "author": "emotz",
  "license": "MIT",
  "devDependencies": {
    "eslint": "^4.5.0"
  }
}
